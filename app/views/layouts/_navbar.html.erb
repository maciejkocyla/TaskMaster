<div class="span12 navbar navbar-inverse">
  <div class="navbar-inner">
  <div class="brand span3"><%= @current_user.name %></div>
    <ul class="nav">
      <li class="divider-vertical divider-inverse"></li>
      <li><%= link_to "callendar", '#'%></li>
      <li><%= link_to "habits", '#'%></li>
      <li><%= link_to "new task", '#'%></li>
      <li><%= link_to @current_task.name, '#' %></li>
      <li class="divider-vertical"></li>
      <li></li>
    </ul>
  </div>
</div>

<ul class="span3 nav nav-list">
  <li class="nav-header"><%= @current_user.tasks.count%> Zadania do wykonania</li>
      <%= render @current_user.tasks.first %>
    <% @current_user.tasks.each do |task|%>
      <%= render task unless task == @current_user.tasks.first || task.completed?%>
    <% end %>
    <li class="nav-header">Ukończone zadania:</li>
    <% @current_user.tasks.each do |task|%>
      <%= render task unless task == @current_user.tasks.first ||  !task.completed? %>
    <% end %>
</ul>

