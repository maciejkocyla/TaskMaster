<div class="span12 container">
<h5>Bierzące zadania:</h5>
<div class="accordion span12" id="accordion2">
  <% current_user.tasks.each do |task|%>
    <% if task.projects.first == @task && !task.completed?%>
      <%= render task%>
    <%end%>
  <%end%>
</div>
<%if @task.content%>
<%end%>
<br />
<h5>Zadania wykonane:</h5>
<div class="accordion span12" id="accordion2">
  <% current_user.tasks.each do |task|%>
    <% if task.projects.first == @task && task.completed?%>
      <%= render task%>
    <%end%>
  <%end%>
</div>
</div>

